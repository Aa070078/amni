# packages/ui — Amni design system

shadcn/ui components + design tokens (colors, typography, spacing, radius, shadows, dark mode), shared data-table core, command palette, wizard, import flow, empty/loading/error states. Storybook for review.

**Rules:** all product UI uses this package; no duplicate implementations in pages; tokens extend only here. See `AGENTS.md §5`.
