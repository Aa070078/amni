# apps/web — Amni product frontend (Next.js)

Next.js App Router frontend. Talks only to the Amni API (`packages/shared` contract). Uses `packages/ui` design system and TanStack Query/Table.

Scaffolded in Phase 2. See `AGENTS.md §14` for how to add a page.
