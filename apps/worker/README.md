# apps/worker — Amni background workers (BullMQ)

Standalone NestJS app consuming queues: `provisioning`, `imports`, `mail`, `notify`, `default`. Drives the provisioning state machine against the ERP cluster.

Scaffolded in Phase 2.
